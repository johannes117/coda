// @ts-nocheck
// Stub for the upstream dev-only react-devtools-core connector (absent from the
// extraction). No-op: coda doesn't attach React DevTools. reconciler.ts only
// imports this for its side effect under NODE_ENV==="development".
export {};
